/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // Backend-only app — no images / no public assets needed.
};

export default nextConfig;
