import type { JobHandler } from '../jobs';

// Placeholder — real implementation lands in the next commit.
export const handleBuildPackage: JobHandler = async () => {
  throw new Error('evidence_package.build handler not implemented yet');
};
