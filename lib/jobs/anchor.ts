import type { JobHandler } from '../jobs';

// Placeholder — real implementation lands in the next commit.
export const handleAnchor: JobHandler = async () => {
  throw new Error('proof.anchor handler not implemented yet');
};
