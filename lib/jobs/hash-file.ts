import type { JobHandler } from '../jobs';

// Placeholder — real implementation lands in the next commit.
export const handleHashFile: JobHandler = async () => {
  throw new Error('proof_file.hash handler not implemented yet');
};
